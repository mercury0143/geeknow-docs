---
title: 文字系列概览
description: 汇总 `router/relay-router.go` 中的文本、聊天、多模态响应、模型列表和网页工具路由。
---

# 文字系列

文字系列覆盖 OpenAI 兼容文本接口、Claude 原生消息接口、Gemini 原生接口、OpenAI Responses、多模态输入、Embedding、Rerank、Moderation 与网页工具。

<CardGroup cols={2}>
  <Card title="通用对话" icon="messages-square" href="/api-reference/texts/chat-completions-stream">
    `POST /v1/chat/completions`，适合聊天、Agent、长文本生成和多模态输入。
  </Card>
  <Card title="OpenAI Responses" icon="bot" href="/api-reference/texts/openai-responses">
    `POST /v1/responses`，适合工具调用、结构化输出和上下文续接。
  </Card>
  <Card title="Claude 原生" icon="file-text" href="/api-reference/texts/claude-messages">
    `POST /v1/messages`，保留 Anthropic Messages 协议。
  </Card>
  <Card title="Gemini 原生" icon="sparkles" href="/api-reference/texts/gemini-native">
    `POST /v1beta/models/{model}:{action}` 与 `GET /v1beta/models`。
  </Card>
</CardGroup>

## 认证方式

大多数文字 relay 接口使用 Bearer Token：

```http
Authorization: Bearer YOUR_API_KEY
```

Claude 原生格式同时兼容：

```http
x-api-key: YOUR_API_KEY
anthropic-version: 2023-06-01
```

Gemini 原生格式同时兼容：

```http
x-goog-api-key: YOUR_API_KEY
```

## 接口清单

| Method | Path | 页面 | 说明 |
| --- | --- | --- | --- |
| `POST` | `/v1/chat/completions` | [通用对话接口（默认流式）](./chat-completions-stream) | OpenAI Chat Completions 兼容格式，文档默认展示流式调用 |
| `POST` | `/v1/chat/completions` | [通用对话接口（默认非流式）](./chat-completions-non-stream) | 同一路由的非流式写法 |
| `POST` | `/v1/completions` | [文本补全接口](./completions) | Legacy Completions 兼容格式 |
| `POST` | `/v1/messages` | [Claude 消息接口](./claude-messages) | Anthropic Messages 原生协议 |
| `POST` | `/v1/responses` | [OpenAI 多模态响应接口](./openai-responses) | Responses API |
| `POST` | `/v1/responses/compact` | [OpenAI 多模态响应接口](./openai-responses#上下文压缩) | 上下文压缩 |
| `POST` | `/v1/embeddings` | [向量接口](./embeddings) | OpenAI Embeddings 兼容格式 |
| `POST` | `/v1/engines/{model}/embeddings` | [向量接口](./embeddings) | Legacy embedding 路径 |
| `POST` | `/v1/rerank` | [重排序接口](./rerank) | 文档相关性重排序 |
| `POST` | `/v1/moderations` | [内容审核接口](./moderations) | OpenAI Moderations 兼容格式 |
| `POST` | `/v1/web-reader/read` | [网页工具接口](./web-tools) | 网页读取 |
| `POST` | `/v1/web-search/search` | [网页工具接口](./web-tools) | 网页搜索 |
| `GET` | `/v1/realtime` | [Realtime 接口](./realtime) | OpenAI Realtime 兼容 WebSocket |
| `GET` | `/v1/models` | [模型列表](./models) | OpenAI/Claude/Gemini 兼容模型列表 |
| `GET` | `/v1/models/{model}` | [模型列表](./models) | 单模型查询 |
| `GET` | `/v1beta/models` | [Gemini 原生格式](./gemini-native) | Gemini 原生模型列表 |
| `POST` | `/v1beta/models/{model}:{action}` | [Gemini 原生格式](./gemini-native) | Gemini 原生推理与图像/视频相关动作 |
| `GET` | `/v1beta/openai/models` | [模型列表](./models) | Gemini 兼容模型列表 |
| `POST` | `/pg/chat/completions` | [通用对话接口（默认非流式）](./chat-completions-non-stream) | Playground 调试路由，使用登录态而非 API Key |

## 通用错误码

| 状态码 | 说明 |
| --- | --- |
| `400` | 请求参数错误，例如模型名不存在、消息结构无效、工具定义不合法 |
| `401` | API Key 缺失、无效或过期 |
| `402` | 额度不足 |
| `413` | 请求体过大 |
| `429` | 请求频率超限 |
| `500` | 服务端内部错误 |
| `503` | 上游不可用或系统临时过载 |

<Tip>
如果接口页里标记了“可选”参数，不代表所有模型都支持该字段。最终是否透传、是否生效，仍取决于实际分发到的渠道能力。
</Tip>

## 推荐阅读顺序

1. [通用对话接口（默认流式）](./chat-completions-stream)
2. [OpenAI 多模态响应接口](./openai-responses)
3. [Claude 消息接口](./claude-messages)
4. [Gemini 原生格式](./gemini-native)
5. [模型列表](./models)
